📌 1. Criar o Banco de Dados sql

CREATE DATABASE meubanco;

🔹 Se quiser usar esse banco imediatamente:

USE meubanco;

📌 2. Criar a Tabela usuarios:

CREATE TABLE usuarios (
    id INT AUTO_INCREMENT PRIMARY KEY,
    nome VARCHAR(100) NOT NULL,
    email VARCHAR(100) NOT NULL UNIQUE,
    password VARCHAR(255) NOT NULL,
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);
🔹 Explicação:

id INT AUTO_INCREMENT PRIMARY KEY → ID único e autoincrementado.
nome VARCHAR(100) NOT NULL → Nome obrigatório.
email VARCHAR(100) NOT NULL UNIQUE → Email único e obrigatório.
password VARCHAR(255) NOT NULL → Senha criptografada.
created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP → Data de criação automática.

📌 3. Inserir um Usuário

INSERT INTO usuarios (nome, email, password) 
VALUES ('João Silva', 'joao@email.com', 'senha123');

🔹 Atenção: Em um sistema real, a senha deve ser armazenada criptografada.

📌 4. Consultar Todos os Usuários:

SELECT * FROM usuarios;

📌 5. Alterar a Estrutura da Tabela:

Adicionar um novo campo (por exemplo, telefone):


ALTER TABLE usuarios ADD telefone VARCHAR(15);

📌 6. Deletar um Usuário Específico:

DELETE FROM usuarios WHERE id = 1;

📌 7. Apagar a Tabela:

DROP TABLE usuarios;

📌 8. Apagar o Banco de Dados:

DROP DATABASE meubanco;
